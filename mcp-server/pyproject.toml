[project]
name = "mcp-knowledge-server"
version = "0.1.0"
description = "Knowledge MCP server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp==2.11.3",
    "sqlmodel",
    "psycopg2-binary",
    "python-dotenv",
]
    